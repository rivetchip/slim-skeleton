

=== REQUEST/RESPONSE ===

- $this->method = $this->filterMethod($method); exeption ! avant le catch par Slim

- mettre des attributes() dans la request/response afin d'y accéder via middle ware ??

- bug upload array files avec des inputs de type : <input type="file" name="my-form[details][avatar]" />


=== SLIM ===

- todo: supprimer la possibilité d'avoir un sous-dossier ?

- todo: quand on précise une route avec un callab,e puis bindTo avec le resolver,
- ça prend le (this) de la route, et pas de slim


- VSCODE : merges : choisir les merges, valider le "changes" dans l'onglet git,
- puis voir les modifs s'il y en a, au cas où certains bouts de codes se seraient rajoutés

- Almost no overhead => 2500+ requests/second ( slim )

- return preg_replace_callback('/(:\w+)/', array(&$this, 'substituteFilter'), $this->url);

- middleswares : add() : faut-il resolver les callable au moment de l'appel à la function ou plutot en defer ?

- $request = self::createRequestFromFactory($_GET, $_POST, array(), $_COOKIE, $_FILES, $_SERVER);

- $request : parseUploadedFiles() utiliser les names de PHp et pas faire de foreach sur cuex existants.

- errors handlers : utiliser un seul array de tout les error handlers, et pas un Notfound, notAllowed, etc...

- error handler : supprimer les renderer, qui ne servent à rien + supprimer la function extraRenderes() inutile...



=== ROUTER ===

- trouver un moyen si l'on délcare les mêmes url, mais pas mêmes methodes : map('get',url_1) map('post',url_1)
ou alors utiliser des uri site/path/create site/path/delete plutôt



=== OTHERS ===


-~{([^}]+)}~

- git merge --no-ff upstream/3.x -s recursive -Xrename-threshold=5%

- use functions instead of controllers ?


- create table 'logs' piwik-like things to do

- event system

- ROUTE GROUP -> APP
